prompt: |
  Extract structured information from the text below.
  Respond ONLY in valid JSON.

  JSON format:
  {
    "skills": ["skill1", "skill2"],
    "persona": "Mentor Material | Needs Guidance | Passive"
  }

  Text:
  "{{bio}}"
